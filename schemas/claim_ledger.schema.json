{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "claim_ledger",
  "description": "Claim ledger is maintained as a markdown table at 07_writing/claim_ledger.md. This schema documents the canonical row shape for tooling.",
  "type": "object",
  "required": ["columns"],
  "properties": {
    "columns": {
      "type": "array",
      "items": {"type": "string"},
      "default": [
        "claim_id",
        "claim_text",
        "evidence_source",
        "strength",
        "paper_location",
        "approved"
      ]
    }
  }
}
